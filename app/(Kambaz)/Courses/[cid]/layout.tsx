
import "bootstrap/dist/css/bootstrap.min.css";
import "./index.css"; // single CSS for everything

export default function CourseLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
